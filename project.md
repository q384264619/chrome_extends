# 需求文档

## 1. 应用概述

### 1.1 应用名称
自动点击助手（浏览器插件）

### 1.2 应用描述
一款浏览器插件，支持主流浏览器（Chrome、Edge、Firefox等），用于根据用户设定的时间、频率、目标元素自动完成点击动作。用户可创建多个任务，每个任务针对单个页面元素，支持单次触发或按日期循环触发。

---

## 2. 用户与使用场景

### 2.1 目标用户
- 需要定时自动点击网页元素的用户
- 需要批量管理多个自动点击任务的用户

### 2.2 核心使用场景
- 定时签到、打卡
- 定时刷新页面特定按钮
- 周期性触发页面交互操作

---

## 3. 页面结构与功能说明

### 3.1 页面结构

```
浏览器插件
├── 插件弹窗页面
│   ├── 任务列表区域
│   └── 添加任务按钮
└── 任务配置页面
    ├── 目标元素设置
    ├── 时间设置
    ├── 频率设置
    ├── 触发模式设置
    └── 保存/取消按钮
```

### 3.2 插件弹窗页面

#### 3.2.1 任务列表区域
- 展示所有已创建的任务
- 每个任务显示：任务名称、目标元素、执行时间、触发模式、状态（启用/禁用）
- 支持启用/禁用任务
- 支持编辑任务
- 支持删除任务

#### 3.2.2 添加任务按钮
- 点击后跳转至任务配置页面

### 3.3 任务配置页面

#### 3.3.1 目标元素设置
- 用户输入CSS选择器（支持ID选择器、类选择器、属性选择器等）
- 示例：#submitBtn、.login-button、[data-action=\"submit\"]
- 每个任务仅支持设置单个目标元素

#### 3.3.2 时间设置
- 用户可选择是否设定执行时间
- 若设定，用户选择具体时间（时:分）
- 若不设定，任务立即执行或根据频率执行

#### 3.3.3 频率设置
- 用户可选择是否设定执行频率
- 若设定，用户输入时间间隔（单位：分钟）
- 若不设定，任务按触发模式执行

#### 3.3.4 触发模式设置
- 单次触发：任务执行一次后自动停止
- 循环触发：任务根据日期循环执行
- 用户选择循环周期：每天、每周（选择星期几）、每月（选择日期）、是否智能跳过节假日期

#### 3.3.5 保存/取消按钮
- 保存：保存任务配置并返回任务列表
- 取消：放弃配置并返回任务列表

---

## 4. 业务规则与逻辑

### 4.1 任务执行规则

#### 4.1.1 时间与频率组合逻辑
- 仅设定时间：在指定时间执行一次
- 仅设定频率：立即执行，并按频率循环执行
- 同时设定时间与频率：在指定时间首次执行，后续按频率循环执行
- 均未设定：立即执行一次
- 开始执行任务后，每隔指定时间刷新页面，以保持某些页面登录状态过期问题
- 所有点击事件必须在页面加载完成之后执行

#### 4.1.2 触发模式执行逻辑
- 单次触发：任务执行一次后状态变为「已完成」，不再执行
- 循环触发：任务根据日期循环周期持续执行，直到用户手动停止

#### 4.1.3 任务执行条件
- 任务状态为「启用」
- 当前页面包含目标元素（通过CSS选择器匹配）
- 满足时间、频率、触发模式条件

### 4.2 任务管理规则
- 用户可同时创建多个任务
- 每个任务独立执行，互不影响
- 任务可随时启用、禁用、编辑、删除

### 4.3 目标元素匹配规则
- 插件在当前页面查找目标元素
- 若找到元素，执行点击动作
- 若未找到元素，任务跳过本次执行

---

## 5. 异常与边界情况

| 场景 | 处理方式 |
|------|----------|
| 目标元素不存在 | 任务跳过本次执行，不报错 |
| CSS选择器格式错误 | 保存时提示用户「选择器格式错误」 |
| 时间格式错误 | 保存时提示用户「时间格式错误」 |
| 频率输入非数字 | 保存时提示用户「频率必须为数字」 |
| 任务列表为空 | 显示「暂无任务，点击添加任务」 |
| 多个任务同时执行 | 按任务创建顺序依次执行 |
| 页面刷新或关闭 | 任务继续在后台执行（若满足条件） |
| 浏览器重启 | 任务配置保留，重启后继续执行 |

---

## 6. 验收标准

1. 用户打开插件弹窗，点击「添加任务」按钮
2. 用户在任务配置页面输入目标元素选择器（如#submitBtn）、设定执行时间（如14:30）、选择触发模式（如单次触发）
3. 用户点击「保存」按钮，返回任务列表，新任务显示在列表中
4. 到达设定时间（14:30），插件自动在当前页面查找目标元素并执行点击动作
5. 任务执行完成后，状态变为「已完成」

---

## 7. 本期不实现功能

- 任务执行日志记录
- 任务执行失败通知
- 任务导入/导出功能
- 任务分组管理
- 目标元素可视化选择（点击页面元素自动生成选择器）
- 任务执行统计与报表
- 多元素批量点击
- 条件触发（如页面加载完成后触发）
- 任务优先级设置
- 跨浏览器任务同步